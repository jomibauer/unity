using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum WeaponStatTypes
{
    LVL, // weapon level eg A, B, C etc
    DAM, // damage
    HIT, // hit %
    CRT, // crit
    WGT, // weight
    DUR, // durability
    RNG, //Range
    SPC, //special... dunno if this is the place
    ELE, // element.  Again, dunno if this is the place.  I spose we can always represent these with numbers!
    Count

}
